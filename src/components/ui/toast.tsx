// Re-export so consumers can import from "@/components/ui/toast"
export { toast, useToastStore } from '../../store/useToastStore';
export type { ToastItem, ToastActionProps } from '../../store/useToastStore';
